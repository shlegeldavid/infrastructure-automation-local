# ===========================================
# Trivy Ignore File
# ===========================================
# -------------------------------------------
# CI/CD файлы с фейковыми credentials
# -------------------------------------------
# Фейковый service_account.json создаётся inline в CI для smoke test.
# Это не реальные credentials — используются только для проверки
# что контейнер запускается без ошибок импорта.
.github/workflows/ci.yml